/* eslint-disable react/prop-types */
import Exp from "./Exp"

const ExpContainer = ({ data }) => {
    return (
        <>
            <Exp data={data} />
        </>
    )
}

export default ExpContainer